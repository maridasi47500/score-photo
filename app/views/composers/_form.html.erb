<%= form_with(model: composer) do |form| %>
  <% if composer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(composer.errors.count, "error") %> prohibited this composer from being saved:</h2>

      <ul>
        <% composer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :country_id, style: "display: block" %>
    <%= form.text_field :country_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
